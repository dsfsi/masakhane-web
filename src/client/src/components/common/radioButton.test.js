import { 
    render, 
    screen, 
    getByRole,
    findByText, 
} from '@testing-library/react';
import RadioButton from './radioButton';

describe('RadioButton', () => {
  test('renders RadioButton component', () => {
    render(<RadioButton />);
  });

//   test('should have a radio button input', () => {
    
//   })

//   test('should fire an onchange event', () => {
    
//   })
  
});
